{
  "pluginAlias": "ShellyDoorbell",
  "pluginType": "platform",
  "singular": true,
  "strictValidation": false,
  "schema": {
    "type": "object",
    "properties": {
      "name": {
        "title": "Nom de la plateforme",
        "type": "string",
        "required": true,
        "default": "Shelly Doorbell"
      },
      "port": {
        "title": "Port du serveur webhook",
        "description": "Port d'écoute du serveur HTTP recevant les webhooks Shelly (partagé par toutes les sonnettes). Défaut : 8081.",
        "type": "integer",
        "default": 8081,
        "minimum": 1,
        "maximum": 65535
      },
      "devices": {
        "title": "Sonnettes",
        "description": "Liste des sonnettes Shelly. Ajoutez une entrée par sonnette (chacune avec son Shelly et, en option, sa caméra).",
        "type": "array",
        "items": {
          "type": "object",
          "properties": {
            "name": {
              "title": "Nom de la sonnette",
              "type": "string"
            },
            "host": {
              "title": "Adresse IP du Shelly",
              "description": "IP locale du dispositif Shelly (ex. 192.168.1.23). Sert aussi d'identifiant de l'accessoire dans HomeKit.",
              "type": "string",
              "required": true
            },
            "streamUrl": {
              "title": "URL du flux vidéo (optionnel)",
              "description": "URL RTSP/HLS/HTTP du flux caméra à exposer dans HomeKit. Le flux est recopié tel quel (-c:v copy), il doit donc être en H.264. Recommandé : pointer vers un restream go2rtc (ex. rtsp://127.0.0.1:8554/<cam>_hk) qui ouvre 1 seule connexion caméra, normalise le codec et gère le crop. Laisser vide pour ne pas activer la caméra (ou activer la caméra de démonstration ci-dessous).",
              "type": "string"
            },
            "useFakeStreamWhenNoUrl": {
              "title": "Activer la caméra de démonstration sans URL personnalisée",
              "description": "Si activé et qu'aucune `streamUrl` n'est fournie, la sonnette expose dans HomeKit un flux de démonstration tournant en boucle (mp4 jour/nuit). Utile pour valider le pipeline HomeKit sans caméra IP réelle.",
              "type": "boolean",
              "default": false
            },
            "maxStreams": {
              "title": "Flux live simultanés",
              "description": "Nombre de consultations live HomeKit en parallèle (Apple TV, iPhone, iPad…). go2rtc fournit déjà le flux H.264 et le plugin ne fait que le recopier (`-c:v copy`, CPU minime), donc plusieurs sessions sont sans risque pour la caméra. Le recadrage et la mise à l'échelle sont gérés en amont par go2rtc. Défaut : 3.",
              "type": "integer",
              "default": 3,
              "minimum": 1,
              "maximum": 6
            }
          }
        }
      }
    }
  }
}
